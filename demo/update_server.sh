#!/bin/bash

# --- ИНСТРУКЦИЯ ---
# Этот скрипт поможет вам быстро обновлять файлы на сервере после того, как вы изменили inventory.js.
# 
# Чтобы использовать этот скрипт, вам нужно настроить доступ к вашему серверу (например, через SSH).
# Если вы используете GitHub Pages или Vercel, лучше использовать автоматический деплой через Git.

# --- НАСТРОЙКИ ---
# Введите данные вашего сервера ниже:
SERVER_USER="your_username"
SERVER_HOST="your_server_ip_or_domain"
SERVER_PATH="/var/www/your_website_folder"

# --- ВЫПОЛНЕНИЕ ---
echo "🚀 Начинаю обновление данных на сервере..."

# Пример обновления через rsync (самый быстрый способ)
# rsync -avz --exclude '.git' --exclude 'update_server.sh' ./ $SERVER_USER@$SERVER_HOST:$SERVER_PATH

# Если вы используете Git, раскомментируйте строки ниже:
# git add inventory.js
# git commit -m "Оновив наявність телефонів"
# git push

echo "✅ Готово! Данные обновлены."
echo "Если вы еще не настроили SSH или Git, отредактируйте этот файл (update_server.sh), чтобы указать ваши данные."
